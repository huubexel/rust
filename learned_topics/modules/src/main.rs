fn main() {
    // Take a look at this code:
    /*
    let plant = Asparagus {};
    println!("I'm growing {:?}!", plant);
    */
    // Because of the use use crate::garden::vegetables::Asparagus; below
    // You can use Asparagus there without problems.
    // The pub mod garden line tells the compiler to include the code it finds in garden.rs


    // Alright the rest of chapter 7 we will skip until we need it because we need a separate
    // setup than just this one big directory.

}

/*
use crate::garden::vegetables::Asparagus;

pub mod garden;
*/
